const DEFAULT_FONT = 'Montserrat';

const shadow1 = {
  shadowColor: '#171717',
  shadowOffset: {width: 0, height: 2},
  shadowOpacity: 0.2,
  shadowRadius: 2,
};

const shadow2 = {
  shadowColor: '#000000',
  shadowOffset: {width: 0, height: 8},
  shadowOpacity: 0.03,
  shadowRadius: 10,
};

const shadow3 = {
  shadowColor: '#000000',
  shadowOffset: {width: 0, height: 6},
  shadowOpacity: 0.1,
  shadowRadius: 5,
};

const shadow4 = {
  shadowColor: '#000000',
  shadowOffset: {width: 0, height: 4},
  shadowOpacity: 0.1,
  shadowRadius: 10,
  elevation: 10,
};

const shadow5 = {
  shadowColor: '#000000',
  shadowOffset: {width: 0, height: 4},
  shadowOpacity: 0.1,
  shadowRadius: 4,
};

const h1 = {
  fontSize: 14,
  fontWeight: '600',
  fontFamily: DEFAULT_FONT,
  color: '#474747',
};

const h2 = {
  fontSize: 12,
  fontWeight: '700',
  fontFamily: DEFAULT_FONT,
  color: '#474269',
};

const p = {
  fontSize: 10,
  fontWeight: '400',
  fontFamily: DEFAULT_FONT,
  color: '#474747',
};

const inputLabel = {
  fontSize: 14,
  fontWeight: '600',
  fontFamily: DEFAULT_FONT,
  color: '#474747',
};

export {DEFAULT_FONT, shadow1, shadow2, shadow3, shadow4, shadow5, h1, h2, p, inputLabel};